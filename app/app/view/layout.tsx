export default async function Layout(props: { children: React.ReactNode}) {
    return (
        <>
            <div className="sm:mb-0 sm:mt-0 mb-20 mt-20">

            </div>
            {props.children}
        </>
    )
}